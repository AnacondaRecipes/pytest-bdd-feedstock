{% set name = "pytest-bdd" %}
{% set version = "8.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/pytest-dev/{{ name }}/archive/refs/tags/{{ version }}.tar.gz
  sha256: 92162b4b92eea7092e07e978b42e552543fe66e4253534567825883cbaf9077b

build:
  number: 0
  skip: true  # [py<39]
  entry_points:
    - pytest-bdd = pytest_bdd.scripts:main
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv

requirements:
  host:
    - python
    - pip
    - poetry-core >=1.0.0
  run:
    - python
    - mako
    - parse
    - parse_type
    - pytest >=7.0.0
    - typing-extensions
    - packaging
    - gherkin-official >=29.0.0

test:
  source_files:
    - tests
    - src
    - pyproject.toml
    - pytest.ini
  imports:
    - pytest_bdd
  requires:
    - pip
  commands:
    - pip check
    - pytest-bdd --help
    - pytest -v tests

about:
  home: https://github.com/pytest-dev/pytest-bdd
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: BDD for pytest
  description: pytest-bdd implements a subset of the Gherkin language to enable automating project requirements testing and to facilitate behavioral driven development.
  dev_url: https://github.com/pytest-dev/pytest-bdd
  doc_url: https://pytest-bdd.readthedocs.io

extra:
  recipe-maintainers:
    - m0nonoke
    - PertuyF
    - breathe
  skip-lints:
    - missing_wheel